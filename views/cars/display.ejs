<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Car Display Page</title>
  </head>
  <body>
    <h1> <%= car.make + " " + car.model %></h1>
      <p> Year: <%= car.year %></p>
      <p> Color: <%= car.color%></p>
      <p> Nickname: <%= car.nickname %></p>
      <% if (car.currentCar) { %>
        <p>This is your current car!</p>
      <% } else { %>
        <p>This is not your current car!</p>
      <% } %>
        <a href="/cars/<%= car._id %>/edit">Edit <%= car.make + " " + car.model%></a>
        <a href="/cars">Browse Cars</a>
        <a href="/">Back to home</a>
  </body>
</html>
